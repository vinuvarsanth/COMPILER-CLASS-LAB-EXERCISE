%{
#include "parser.tab.h"  // Header file generated by Bison
%}

%%

[a-zA-Z_][a-zA-Z_0-9]*  { return id; }          // Identifier
[0-9]+(\.[0-9]*)?        { return num; }         // Number (integer or floating point)
[+\-/*]                 { return op; }          // Operator

.                       { return yytext[0]; }   // Any other character (like parentheses, etc.)
\n                      { return 0; }           // End of input (newline)

%%

int yywrap() {
    return 1;
}
